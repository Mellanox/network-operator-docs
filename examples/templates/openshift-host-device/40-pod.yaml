apiVersion: v1
kind: Pod
metadata:
  name: hostdev-rdma-server
  namespace: default
  labels:
    app: hostdev-rdma
    role: server
  annotations:
    k8s.v1.cni.cncf.io/networks: hostdev-net
spec:
  restartPolicy: Never
  containers:
  - name: rdma-test
    image: |doca-workload-image|
    command: ["/bin/bash", "-c", "sleep infinity"]
    securityContext:
      capabilities:
        add: ["IPC_LOCK"]
    resources:
      requests:
        nvidia.com/hostdev: "1"
      limits:
        nvidia.com/hostdev: "1"
---
apiVersion: v1
kind: Pod
metadata:
  name: hostdev-rdma-client
  namespace: default
  labels:
    app: hostdev-rdma
    role: client
  annotations:
    k8s.v1.cni.cncf.io/networks: hostdev-net
spec:
  restartPolicy: Never
  affinity:
    podAntiAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
      - labelSelector:
          matchExpressions:
          - key: role
            operator: In
            values:
            - server
        topologyKey: kubernetes.io/hostname
  containers:
  - name: rdma-test
    image: |doca-workload-image|
    command: ["/bin/bash", "-c", "sleep infinity"]
    securityContext:
      capabilities:
        add: ["IPC_LOCK"]
    resources:
      requests:
        nvidia.com/hostdev: "1"
      limits:
        nvidia.com/hostdev: "1"
